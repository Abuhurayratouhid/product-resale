import { useQuery } from '@tanstack/react-query';
import React from 'react';

const Sellers = () => {
    // const {} = useQuery({
    //     queryKey: ['sellers'],
    //     queryFn: ()=>fetch('')
    // })
    return (
        <div>
            <h1>Sellers </h1>
        </div>
    );
};

export default Sellers;